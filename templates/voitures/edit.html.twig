{% extends 'base.html.twig' %}

{% block title %}Edit Voitures{% endblock %}
{% block body %}
    <h1>Edit Voitures</h1>
    {{ include('voitures/_form.html.twig', {'button_label': 'Update'}) }}

    <a class = "backToList" href="{{ path('home') }}">Revenir à la page d'accueil</a>

    {{ include('voitures/_delete_form.html.twig') }}
			{{ encore_entry_script_tags('formCars') }}
            {{encore_entry_link_tags("formCars")}}
              <script>
              const body = document.body
               const form = document.querySelector('form')
                    if(window.location.href.indexOf("edit") > -1) {
                         body.style.padding = "0 13%"
                    }
               </script>

{% endblock %}
