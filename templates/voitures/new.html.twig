{% extends 'base.html.twig' %}

{% block title %}New Voitures{% endblock %}

{% block body %}
     <a href="{{path('app_user', {slug: user.slug})}}">Revenir sur mon profil</a>
    <pre>
        {{ dump() }}
    </pre>
    <h1>Create new Voitures</h1>

    {{ include('voitures/_form.html.twig') }}
{% endblock %}
